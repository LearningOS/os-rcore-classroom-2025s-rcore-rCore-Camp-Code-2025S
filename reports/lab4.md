# 总结

本章节实验完成了三个系统调用，实现文件的硬链接和解除，获取文件的状态信息。实现函数需要对ease-fs进行修改，添加一些辅助函数帮助我获取需要的对象信息，以便找到对应的结构修改，添加。

# 问答题

ch6

root_inode相当于平时使用的根目录，一切的文件操作都要从这里开始出发，一旦损坏，文件系统会变得无法使用，入口点丢失，一切操作都将无法进行。

ch7

一、在 Linux 终端中，pipe 是将一个程序的输出连接到另一个程序的输入的常用方式，例子如下：
```
cat myfile.txt | wc -l
```

二、可以使用消息队列通信方式。